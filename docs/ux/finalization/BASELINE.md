# UI/UX Finalization Baseline

## 기준

- 기준 브랜치: `codex/ui-ux-audit-improvements`
- 기준 커밋: `437219d`
- 전체 테스트: `517 passed, 2 skipped`
- 자동 화면 캡처: 한국어·영어 합계 `16 / 16`
- 자동 캡처가 증명하는 범위: 화면 생성, 지정 픽셀 크기 렌더링, 주요 위젯 생성, 예외 없음
- 자동 캡처가 증명하지 않는 범위: 실제 Windows 125% DPI에서의 글자 잘림·겹침·포커스 이동

## 재현된 결함

| 결함 | 영향 화면 | 수정 작업 |
|---|---|---|
| 가져오기 카드의 단계 버튼이 작업별 실제 상태 전이 없이 같은 앱 동작으로 라우팅됨 | 가져오기 센터 | F1-1, F1-2, F1-3 |
| 대시보드 단계가 경로 존재 여부와 고정 상태 문구에 의존함 | 대시보드 | F2-1, F2-2 |
| 앱이 메시지 검토 화면에 재분석 콜백을 제공하지 않음 | 뉴스 메시지 검토 | F3-1 |
| 처리 완료 메시지를 식별하는 영구 상태가 없어 재스캔 중복을 확실히 차단하지 못함 | 뉴스 메시지 검토, 달성 기록 | F3-2, F3-3 |
| 메시지 유형·제외 사유·추출 필드의 내부 코드가 사용자 경로에 노출될 수 있음 | 뉴스 메시지 검토 | F4 |
| 메시지 추출 수정이 수동 입력의 안내형 폼과 검증을 공유하지 않음 | 뉴스 메시지 검토, 수동 기록 | F5 |
| 기존 `is_manual=1` 메시지 자동 기록을 `manual`로 분류할 수 있음 | 달성 기록, 마이그레이션 | F6 |
| GitHub Actions 워크플로가 없어 재현 가능한 CI 근거가 없음 | 저장소 전체 | F8 |

## 작업 소유권

| 작업 ID | 담당 모델 | 소유 파일 | 읽기 전용 참조 | 수정 금지 | 선행 작업 | 완료 조건 |
|---|---|---|---|---|---|---|
| A/C | GPT-5.5 | `core/app_state.py`, `core/import_workflow.py`, `core/milestone/message_automation/`, 전용 테스트 | 기존 DB·parser API | `gui/`, `core/i18n/`, `core/db/schema.py` | 없음 | 영구 작업 상태, 처리 메시지 상태, 행별 적용 결과 테스트 |
| B | Sonnet 5 | `gui/views/import_center_view.py`, `gui/widgets/import_workflow_status.py`, `gui/views/dashboard_view.py`, 전용 UI 테스트 | 상태 계약 | `gui/app.py`, `core/` | A의 상태 계약 | 단계별 라우팅, 상태 기반 활성화, 대시보드 계산 |
| D/F4/F5 | Sonnet 5 | 메시지 검토·공통 폼·현지화 파일과 전용 테스트 | parser·recorder API | `gui/app.py`, DB schema, 가져오기 화면 | C의 결과 계약 | 재분석·필터·행별 결과·안내형 폼·동적 현지화 |
| 통합/F6/F8 | GPT-5.6 Sol | `gui/app.py`, `core/db/schema.py`, CI, 검증·보고 문서 | 모든 구현 결과 | 에이전트 소유 파일의 동시 수정 | A~D | 전체 회귀, 마이그레이션, CI, 사실 기반 최종 보고 |

## 검수용 고정 시나리오

자동 테스트 fixture로 신규·제외·날짜 누락·파싱 오류·기존 반영 메시지와 기존 수동·메시지 자동·시즌 최종 기록을 사용한다. 박스스코어는 성공과 부분 오류 상태를 별도 작업 실행 상태로 검증한다.
